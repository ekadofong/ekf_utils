import setuptools

setuptools.setup (
    name = 'ekf_utils',
    version='0.0.1',
    author='Erin Kado-Fong',
    description='Utilities to do little things for me',
    packages=setuptools.find_packages(),
    python_requires='>=3.0'
    )
